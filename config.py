# Database configuration
DB_URL = 'path_to_your_sqlite_db'

# API configuration
OPENAI_API_KEY = 'your_api_key'

# Number of retries if a completion fails (eg. wrong/broken format)
RETRIES_TO_ABORT='no_of_tries'
